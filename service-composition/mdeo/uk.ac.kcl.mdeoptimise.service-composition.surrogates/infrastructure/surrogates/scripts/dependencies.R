# Install the required dependencies for R to build the service composition case study
# surrogate models

install.packages(c("foreach", "leaps", "earth", "DAAG", "caret", "e1071", "rpart", "languageR", "randomForest", "MASS", "party", "rJava"))